# 🌍 Руководство по многоязычности Smart Care

## Обзор

Smart Care теперь поддерживает **два языка**:
- 🇷🇺 **Русский** (по умолчанию)
- 🇬🇧 **Английский**

Все тексты хранятся в **Firebase Firestore** и могут быть обновлены без изменения кода.

---

## 🎯 Функционал

### 1. Переключатель языка

#### Desktop версия (верхний header):
```
┌─────────────────────────────────────────────────────────────┐
│ 🛡️ Smart Care  [Главная] [Команда] [...]  [🇷🇺 RU] [🇬🇧 EN] │
└─────────────────────────────────────────────────────────────┘
```

#### Mobile версия (правый верхний угол):
```
                                                    ┌──────┐
                                                    │🇷🇺 🇬🇧│
                                                    └──────┘
```

### 2. URL структура

- **Русский**: `http://localhost:5001/` или `http://localhost:5001/ru`
- **English**: `http://localhost:5001/en`

### 3. Автоопределение языка

Если пользователь заходит первый раз:
- Проверяется `Accept-Language` header браузера
- Если браузер английский → автоматически `en`
- Иначе → `ru` (по умолчанию)

---

## 🗂️ Структура данных в Firestore

### Collections

```
firestore/
├── translations/           # Статичные переводы
│   ├── ru_navigation      # Навигация (RU)
│   ├── en_navigation      # Навигация (EN)
│   ├── ru_hero            # Главная страница (RU)
│   ├── en_hero            # Главная страница (EN)
│   ├── ru_problem         # Проблема (RU)
│   ├── en_problem         # Проблема (EN)
│   └── ...
│
├── team_members/          # Команда
│   ├── member_1           # { ru: {...}, en: {...} }
│   └── member_2
│
├── roadmap_milestones/    # Этапы дорожной карты
│   ├── milestone_1        # { ru: {...}, en: {...} }
│   └── ...
│
└── roadmap_next_steps/    # Следующие шаги
    ├── step_1             # { ru: {...}, en: {...} }
    └── ...
```

### Пример документа с переводом

```json
{
  "ru": {
    "title": "Исследование и концепция",
    "date": "Ноябрь 2025",
    "description": "Анализ проблемы, исследование рынка, разработка концепции",
    "status": "completed"
  },
  "en": {
    "title": "Research and Concept",
    "date": "November 2025",
    "description": "Problem analysis, market research, concept development",
    "status": "completed"
  }
}
```

---

## 🛠️ Как использовать в шаблонах

### Простой текст

```html
<h2>{{ translations.navigation.home|default('Главная') }}</h2>
```

- `translations.navigation.home` → берется из Firestore
- `default('Главная')` → fallback, если Firestore недоступен

### Список данных (команда, roadmap)

```html
{% for member in team %}
    <h3>{{ member.name }}</h3>
    <p>{{ member.role }}</p>
{% endfor %}
```

Данные автоматически фильтруются по языку в `app.py`:

```python
team = firestore_service.get_collection('team_members', lang='ru')
# Вернет только { ru: {...} } данные
```

---

## 📡 API эндпоинты

### Переключение языка
```
GET /set-language/<lang>
```

Примеры:
- `/set-language/ru` → переключает на русский
- `/set-language/en` → переключает на английский

### Получение данных по языку

```
GET /api/team/<lang>         # Команда
GET /api/roadmap/<lang>      # Дорожная карта
GET /api/translations/<lang> # Все переводы
```

Примеры:
```bash
# Получить команду на английском
curl http://localhost:5001/api/team/en

# Получить переводы на русском
curl http://localhost:5001/api/translations/ru
```

### Очистка кэша (только в DEBUG режиме)

```
GET /api/clear-cache
```

---

## 🔧 Добавление нового языка

### Шаг 1: Обновить config.py

```python
SUPPORTED_LANGUAGES = ['ru', 'en', 'uz']  # Добавили узбекский
```

### Шаг 2: Добавить данные в Firestore

Для каждого документа добавить поле `uz`:

```python
{
  "ru": { ... },
  "en": { ... },
  "uz": { ... }  # ← Новый язык
}
```

### Шаг 3: Добавить кнопку в header

```html
<button class="lang-btn {% if current_lang == 'uz' %}active{% endif %}" 
        onclick="window.location.href='{{ url_for('set_lang', lang='uz') }}'">
    🇺🇿 UZ
</button>
```

---

## 🎨 Стилизация переключателя

### Desktop стили (.language-switcher)

```css
.language-switcher {
    display: flex;
    gap: 8px;
    margin-left: auto;
}

.lang-btn {
    background: var(--light-gray);
    color: var(--text);
    border: 2px solid transparent;
    padding: 8px 16px;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 600;
}

.lang-btn.active {
    background: var(--primary-blue);
    color: var(--white);
}
```

### Mobile стили (.mobile-language-switcher)

```css
.mobile-language-switcher {
    position: fixed;
    top: 10px;
    right: 10px;
    display: flex;
    gap: 8px;
    z-index: 1500;
}

.lang-btn-mobile {
    background: var(--light-gray);
    padding: 6px 10px;
    border-radius: 8px;
    font-size: 1.2rem;
}
```

---

## 💾 Кэширование

### Настройки кэша

```python
# config.py
CACHE_TYPE = 'simple'
CACHE_DEFAULT_TIMEOUT = 3600  # 1 час
```

### Кэшируемые данные

- ✅ `get_team_from_firestore(lang)` → 1 час
- ✅ `get_roadmap_from_firestore(lang)` → 1 час
- ✅ `get_translations(lang)` → 1 час
- ✅ `firestore_service.get_document()` → lru_cache

### Очистка кэша

```python
# Вручную в коде
cache.clear()
firestore_service.clear_cache()

# Через API (только в DEBUG)
curl http://localhost:5001/api/clear-cache
```

**Автоматическая очистка**: при смене языка кэш очищается автоматически.

---

## 🧪 Тестирование

### 1. Проверить переключатель

```bash
# Открыть сайт
open http://localhost:5001

# Кликнуть на 🇬🇧 EN
# Проверить, что URL изменился на /en
# Проверить, что тексты изменились на английский
```

### 2. Проверить API

```bash
# Команда на русском
curl http://localhost:5001/api/team/ru | jq

# Команда на английском
curl http://localhost:5001/api/team/en | jq
```

### 3. Проверить автоопределение

```bash
# С английским браузером
curl -H "Accept-Language: en-US,en;q=0.9" http://localhost:5001
# Должен перенаправить на /en

# С русским браузером
curl -H "Accept-Language: ru-RU,ru;q=0.9" http://localhost:5001
# Должен показать /ru
```

---

## ⚠️ Troubleshooting

### Проблема: Переводы не загружаются

**Решение**:
1. Проверить, что Firestore инициализирован:
   ```bash
   curl http://localhost:5001/api/health
   # {"firestore_available": true}
   ```

2. Проверить логи:
   ```bash
   tail -f app.log
   ```

3. Очистить кэш:
   ```bash
   curl http://localhost:5001/api/clear-cache
   ```

### Проблема: Язык не сохраняется

**Причина**: Sessions требуют `SECRET_KEY`

**Решение**:
```python
# config.py
SECRET_KEY = 'your-secret-key-here'
```

### Проблема: Fallback данные показываются вместо Firestore

**Причина**: Firestore недоступен или данные не мигрированы

**Решение**:
```bash
# Запустить миграцию заново
python migrate_to_firestore.py
```

---

## 📝 Checklist для добавления нового перевода

- [ ] Создать документ в Firestore с полями `ru` и `en`
- [ ] Обновить `migrate_to_firestore.py` с новыми данными
- [ ] Добавить fallback данные в `app.py` (LOCAL_*)
- [ ] Обновить шаблон с `{{ translations.*.* }}`
- [ ] Добавить `|default('...')` для fallback
- [ ] Протестировать оба языка
- [ ] Очистить кэш после изменений

---

## 🚀 Производственные рекомендации

1. **Используйте Redis** для кэширования вместо `simple`:
   ```python
   CACHE_TYPE = 'redis'
   CACHE_REDIS_URL = 'redis://localhost:6379/0'
   ```

2. **Увеличьте timeout** для production:
   ```python
   CACHE_DEFAULT_TIMEOUT = 86400  # 24 часа
   ```

3. **Мониторьте Firestore usage** через Firebase Console

4. **Создайте admin панель** для управления переводами (см. следующий этап)

---

## 📚 Дополнительные ресурсы

- [Flask-Caching Docs](https://flask-caching.readthedocs.io/)
- [Firebase Firestore Python SDK](https://firebase.google.com/docs/firestore/quickstart)
- [i18n Best Practices](https://phrase.com/blog/posts/i18n-best-practices/)

---

**Автор**: Smart Care Team  
**Дата**: Ноябрь 2025  
**Версия**: 1.0.0

