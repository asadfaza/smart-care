# 🚀 Quick Start Guide - Safe Care

## Быстрый запуск за 3 минуты

### 1️⃣ Убедитесь, что Python установлен

```bash
python3 --version
# Должно показать: Python 3.x.x
```

### 2️⃣ Перейдите в папку проекта

```bash
cd "/Users/asadfaza/Documents/Grad Project/application/safe_care"
```

### 3️⃣ Активируйте виртуальное окружение

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

После активации вы увидите `(venv)` в начале командной строки.

### 4️⃣ Запустите приложение

```bash
python app.py
```

или

```bash
python3 app.py
```

### 5️⃣ Откройте в браузере

Откройте ваш браузер и перейдите по адресу:

```
http://127.0.0.1:5001
```

или

```
http://localhost:5001
```

## ✅ Готово!

Теперь вы можете:
- 📱 **На мобильном**: использовать табы внизу экрана
- 🖥️ **На desktop**: использовать стрелки или клавиатуру (← →)
- 🔄 **Переворачивать карточки команды**: клик или наведение мыши

---

## 🛑 Остановка сервера

Нажмите `Ctrl + C` в терминале, где запущен сервер.

## 🔄 Перезапуск после изменений

1. Остановите сервер (`Ctrl + C`)
2. Снова запустите `python app.py`

---

## ❗ Возможные проблемы

### Проблема: "command not found: python"
**Решение:** Используйте `python3` вместо `python`

### Проблема: "ModuleNotFoundError: No module named 'flask'"
**Решение:** Убедитесь, что виртуальное окружение активировано (см. шаг 3)

### Проблема: "Address already in use"
**Решение:** 
```bash
# Найдите процесс на порту 5001
lsof -i :5001
# Остановите его
kill -9 <PID>
```

### Проблема: Порт 5001 занят
**Решение:** Измените порт в `config.py`:
```python
PORT = 5002  # Или любой другой свободный порт
```

---

## 📝 Кастомизация

### Изменить данные команды
Откройте `app.py` и отредактируйте `TEAM_DATA`

### Изменить цвета
Откройте `config.py` и отредактируйте `COLORS`

### Добавить контент
Откройте `templates/index.html`

---

## 🎯 Для презентации

1. ✅ Откройте на **fullscreen** (F11)
2. ✅ Используйте **клавиатурную навигацию** (← →)
3. ✅ Покажите **responsive** (Developer Tools, Ctrl+Shift+M)
4. ✅ Продемонстрируйте **flip-карточки команды**

---

**Удачи на хакатоне! 🏆**

