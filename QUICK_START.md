# 🚀 Quick Start - Smart Care

## Быстрый запуск приложения

### 1. Активация виртуального окружения

```bash
# macOS/Linux
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### 2. Проверка установленных зависимостей

```bash
pip list | grep -E "flask|firebase|python-dotenv"
```

Должны быть установлены:
- `Flask` (3.1.2)
- `firebase-admin` (6.3.0)
- `Flask-Caching` (2.1.0)
- `python-dotenv` (1.0.0)

### 3. Проверка Firebase credentials

```bash
ls -la firebase-credentials.json
```

Убедитесь, что файл существует и содержит валидные credentials от Firebase.

### 4. Запуск приложения

```bash
python app.py
```

Вы должны увидеть:

```
============================================================
  🛡️  SMART CARE - Запуск приложения
============================================================

✓ Версия: 1.0.0
✓ Debug режим: True
✓ Firestore: ✓ Подключен
✓ Поддерживаемые языки: ru, en
✓ Язык по умолчанию: ru

🌐 Приложение доступно по адресу: http://127.0.0.1:5001
🌍 Русский: http://localhost:5001/ru
🌍 English: http://localhost:5001/en

============================================================
```

---

## 🧪 Тестирование функционала

### Тест 1: Главная страница

```bash
# Открыть в браузере
open http://localhost:5001
```

**Ожидаемый результат**:
- ✅ Загружается главная страница
- ✅ Видно переключатель языка (🇷🇺 RU | 🇬🇧 EN)
- ✅ Навигация работает (header desktop, tabs mobile)
- ✅ Все секции отображаются корректно

### Тест 2: Переключение языка

1. **Кликнуть на 🇬🇧 EN**
   - URL должен измениться на `/en`
   - Все тексты должны стать английскими
   
2. **Кликнуть на 🇷🇺 RU**
   - URL должен измениться на `/ru`
   - Все тексты должны вернуться на русский

### Тест 3: API эндпоинты

```bash
# Проверка health
curl http://localhost:5001/api/health | jq

# Ожидаемый ответ:
# {
#   "status": "healthy",
#   "service": "smart_care",
#   "version": "1.0.0",
#   "firestore_available": true
# }

# Команда на русском
curl http://localhost:5001/api/team/ru | jq

# Команда на английском
curl http://localhost:5001/api/team/en | jq

# Roadmap
curl http://localhost:5001/api/roadmap/ru | jq

# Переводы
curl http://localhost:5001/api/translations/ru | jq
```

### Тест 4: Admin панель

```bash
# Открыть admin панель (только в DEBUG режиме)
open http://localhost:5001/admin
```

**Ожидаемый результат**:
- ✅ Открывается admin интерфейс
- ✅ Видны табы: Переводы, Команда, Дорожная карта, Настройки
- ✅ Firebase Status показывает "✓ Firebase подключен"
- ✅ Можно переключаться между языками

### Тест 5: Мобильная версия

1. **Открыть DevTools** (F12)
2. **Toggle Device Toolbar** (Ctrl+Shift+M)
3. **Выбрать iPhone/Android**

**Проверить**:
- ✅ Мобильные табы внизу экрана
- ✅ Переключатель языка в правом верхнем углу (🇷🇺 🇬🇧)
- ✅ Все секции прокручиваются корректно
- ✅ Карточки команды флипаются по клику

### Тест 6: 404/500 страницы

```bash
# Тест 404
open http://localhost:5001/nonexistent-page

# Тест переводов на 404
open http://localhost:5001/nonexistent-page?lang=en
```

**Ожидаемый результат**:
- ✅ Красивая 404 страница
- ✅ Тексты на правильном языке
- ✅ Кнопка "Вернуться на главную" работает

---

## 📊 Проверка Firestore данных

### Через Firebase Console

1. Открыть [Firebase Console](https://console.firebase.google.com/)
2. Выбрать проект `smartt-care`
3. Перейти в **Firestore Database**

**Проверить collections**:
- ✅ `translations` (14+ документов)
- ✅ `team_members` (2 документа)
- ✅ `roadmap_milestones` (4 документа)
- ✅ `roadmap_next_steps` (4 документа)

### Через API

```bash
# Проверить, что данные загружаются из Firestore
curl http://localhost:5001/api/team/ru | jq '.[] | .name'

# Должен вернуть:
# "Асадбек Фазлиддинов"
# "Сайдулло Султонов"
```

---

## 🐛 Troubleshooting

### Проблема: "ModuleNotFoundError: No module named 'firebase_admin'"

**Решение**:
```bash
pip install firebase-admin==6.3.0 Flask-Caching==2.1.0 python-dotenv==1.0.0
```

### Проблема: "Firestore: ✗ Не подключен"

**Причины**:
1. Файл `firebase-credentials.json` не найден
2. Credentials невалидны
3. Firestore database не создана

**Решение**:
```bash
# Проверить файл
cat firebase-credentials.json

# Проверить в Firebase Console
# Database → Firestore Database → должна быть Native mode
```

### Проблема: "Переводы не загружаются"

**Решение**:
```bash
# 1. Проверить миграцию
python migrate_to_firestore.py

# 2. Очистить кэш
curl http://localhost:5001/api/clear-cache

# 3. Перезапустить приложение
# Ctrl+C
python app.py
```

### Проблема: "Admin панель недоступна"

**Причина**: Работает только в DEBUG режиме

**Решение**:
```python
# config.py
class DevelopmentConfig(Config):
    DEBUG = True  # Должно быть True
```

### Проблема: "Язык не сохраняется между страницами"

**Причина**: Sessions требуют SECRET_KEY

**Решение**:
```python
# config.py
SECRET_KEY = 'your-secret-key-here'
```

---

## ✅ Checklist готовности

- [ ] Виртуальное окружение активировано
- [ ] Все зависимости установлены
- [ ] Firebase credentials на месте
- [ ] Firestore database создана (Native mode)
- [ ] Миграция данных выполнена успешно
- [ ] Приложение запускается без ошибок
- [ ] Firestore подключен (check /api/health)
- [ ] Переключатель языка работает
- [ ] Все API endpoints отвечают
- [ ] Admin панель доступна
- [ ] Мобильная версия работает корректно

---

## 📱 Демо URL

После запуска приложения доступны следующие URL:

| URL | Описание |
|-----|----------|
| `http://localhost:5001/` | Главная (русский) |
| `http://localhost:5001/ru` | Главная (русский) |
| `http://localhost:5001/en` | Главная (английский) |
| `http://localhost:5001/admin` | Admin панель |
| `http://localhost:5001/api/health` | Health check |
| `http://localhost:5001/api/team/ru` | Команда (API) |
| `http://localhost:5001/api/roadmap/en` | Roadmap (API) |
| `http://localhost:5001/api/translations/ru` | Переводы (API) |
| `http://localhost:5001/api/clear-cache` | Очистить кэш |

---

## 🎯 Что дальше?

1. **Production deployment**:
   - Настроить Redis для кэширования
   - Добавить аутентификацию для admin панели
   - Настроить HTTPS
   - Использовать Gunicorn/uWSGI

2. **Улучшения**:
   - Добавить больше языков
   - Создать полнофункциональную admin панель с редактированием
   - Добавить логирование действий пользователей
   - Интегрировать analytics

3. **Тестирование**:
   - Написать unit тесты
   - Добавить integration тесты
   - Настроить CI/CD

---

**Удачи в разработке! 🚀**

*Smart Care Team*

